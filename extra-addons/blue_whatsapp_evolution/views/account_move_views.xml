<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Agregar el botón de WhatsApp a la vista de formulario de facturas -->
        <record id="view_move_form_inherit_whatsapp" model="ir.ui.view">
            <field name="name">account.move.view.form.inherit.whatsapp</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_send_msg" string="Enviar WhatsApp"
                            type="object"
                            class="oe_stat_button" icon="fa-whatsapp"
                            invisible="not partner_id"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
